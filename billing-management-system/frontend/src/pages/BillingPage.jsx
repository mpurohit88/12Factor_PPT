export default function BillingPage() {
  return (
    <section className="card">
      <h2>Billing & Accounts</h2>
      <p>
        Backend APIs are ready for invoice creation at <code>/api/billing/invoices</code>.
        Extend this page to add invoice entry and payment tracking UI.
      </p>
    </section>
  );
}
